"""
example_1
 괄호가 잘 써졌는지 확인하는 예제

psudo code

While # of parenthesis:
    if symbol is opening:
        push opening symbol to stack

    else(closing symbol):
        pop opening symbol from stack

is_empty -> true가 나오면 괄호가 잘 열리고 닫혔다.

"""

"""
exampel_2
10잔수를 2진수 코드로 바꾸는 코드

자연어로 표현
10진수 값을 2로 나눈 나머지 값을 stack에 저장
10진수가 0이 될때까지 나눈다.

psudo code

while 10진수 > 0:
    나머지 = 10진수 % 2
    나머지


"""
